<Window
    x:Class="事件转命令.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:事件转命令"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Window1"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance local:MyViewModel}"
    mc:Ignorable="d">
    <Window.Resources>
        <local:MyValueConverter x:Key="d"/>
    </Window.Resources>
    <StackPanel>


        <!--<DataGrid AutoGenerateColumns="False"
            Width="180"
            Height="180"
            ItemsSource="{Binding Path=Students}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Name">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding Name ,Mode=TwoWay,UpdateSourceTrigger=LostFocus}"  ItemsSource="{Binding Path=DataContext.Options, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGrid}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding Path=Age,Mode=TwoWay}" Header="Age"   />
            </DataGrid.Columns>
        </DataGrid>-->
    </StackPanel>
</Window>
